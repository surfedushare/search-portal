aws:
  harvest_content_bucket: "surfpol-harvester-content-dev"  # TODO: needs renaming

postgres:
  database: "harvester_publinova"
  user: "harvester_publinova"
  application_user: "harvester_publinova"

opensearch:
  alias_prefix: "publinova"

django:
  fixtures:
    - "core/fixtures/initial-nppo.json"  # needs renaming
    - "metadata/fixtures/initial-metadata-nppo.json"
