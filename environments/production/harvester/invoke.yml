project: "harvester"

django:
  directory: "harvester"
  fixtures:
    - "core/fixtures/datasets-history.json"
    - "edurep/fixtures/surf-oaipmh-1970-01-01.json"
  redis_host: "harvester.o1edd0.0001.euc1.cache.amazonaws.com:6379"

postgres:
  database: "harvester"
  user: "harvester"
  application_user: "harvester"

schedule:
  harvest:
    hour: 5  # uses UTC
    minute: 0
    role: "primary"

aws:
  harvest_content_bucket: "surfpol-harvester-content-prod"
  task_role_arn: "arn:aws:iam::017973353230:role/ecsHarvesterTaskExecutionRole"

secrets:
  postgres:
    password: "qwerty"  # TODO: create
    application_password: "qwerty"  # TODO: create
  django:
    secret_key: "development-secret-key--do-not-use"  # TODO: create
    admin_password: "qwerty"  # TODO: create
